import { GetLastUpdateRepoPipe } from './get-last-update-repo.pipe';

describe('GetLastUpdateRepoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLastUpdateRepoPipe();
    expect(pipe).toBeTruthy();
  });
});
